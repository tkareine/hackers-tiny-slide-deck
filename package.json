{
  "name": "hackers-tiny-slide-deck",
  "private": true,
  "scripts": {
    "clean": "rm -fr build node_modules",
    "lib:build:css": "mkdir -p build && node-sass --output-style compressed htsd.css > build/htsd.min.css",
    "lib:build:js": "mkdir -p build && terser --compress --comments -- htsd.js > build/htsd.min.js",
    "lib:build": "npm run lib:build:css && npm run lib:build:js",
    "example:build": "mkdir -p build && marked --gfm -i example.md > build/example.html",
    "example:publish": "git checkout master && git checkout -B tmp-gh-pages && npm run lib:build && npm run example:build && cp htsd.{css,js} build/ && git add -f build && git commit -m 'Generated example' && git subtree split --prefix build -b gh-pages && git push -f origin gh-pages && git branch -D gh-pages && git checkout master"
  },
  "devDependencies": {
    "marked": "^0.7.0",
    "node-sass": "^4.13.0",
    "terser": "^4.4.0"
  },
  "engines": {
    "node": ">= 12"
  }
}
